.idea
.phpunit.cache
build
composer.lock
coverage
phpunit.xml
phpstan.neon
vendor
node_modules

# Note: testbench.yaml is deliberately NOT ignored — it defines the panel the workbench
# boots. The sqlite database it generates lives under vendor/, already ignored above.
