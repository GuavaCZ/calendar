---
title: Events
order: 20
---

# Events

Events are what the calendar is all about. You provide them through the `getEvents` method on your widget, either straight from eloquent or as `CalendarEvent` objects you build yourself.

The following pages cover both ways, all the options an event supports and how to render events with your own content.
